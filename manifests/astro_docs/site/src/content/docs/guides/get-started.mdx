---
title: Get Started
description: First steps for editing and publishing documentation.
---

## Edit locally

Starlight will hot reload when files inside `src/content/docs/` change during `npm run dev`.

## Build for production

The production image still builds static files and serves them with nginx.

## Add more pages

Create more `.mdx` files under `src/content/docs/guides/` or other subdirectories.
